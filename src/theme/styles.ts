import {makeStyles} from "@material-ui/core/styles";

export const globalStyles = makeStyles((theme) => ({
    noShadow: {
        boxShadow: 'none'
    },

    bold: {
        fontWeight: 'bold'
    },

    capitalize: {
        textTransform: 'none'
    }
}))